#include "bubble.h"
#include "../platform/platform.h"
#include "../util/mem.h"
#include "../util/log.h"

#include <string.h>

// 5x7 ASCII bitmap font (printable 32..126).
static const uint8_t FONT[96][7] = {
    {0x00,0x00,0x00,0x00,0x00,0x00,0x00}, {0x00,0x00,0x00,0x5F,0x00,0x00,0x00},
    {0x00,0x00,0x07,0x00,0x07,0x00,0x00}, {0x00,0x14,0x7F,0x14,0x7F,0x14,0x00},
    {0x24,0x2A,0x7F,0x2A,0x12,0x00,0x00}, {0x23,0x13,0x08,0x64,0x62,0x00,0x00},
    {0x36,0x49,0x55,0x22,0x50,0x00,0x00}, {0x00,0x00,0x07,0x00,0x00,0x00,0x00},
    {0x00,0x1C,0x22,0x41,0x00,0x00,0x00}, {0x00,0x41,0x22,0x1C,0x00,0x00,0x00},
    {0x08,0x2A,0x1C,0x2A,0x08,0x00,0x00}, {0x08,0x08,0x3E,0x08,0x08,0x00,0x00},
    {0x00,0x50,0x30,0x00,0x00,0x00,0x00}, {0x08,0x08,0x08,0x08,0x08,0x00,0x00},
    {0x00,0x60,0x60,0x00,0x00,0x00,0x00}, {0x20,0x10,0x08,0x04,0x02,0x00,0x00},
    {0x3E,0x51,0x49,0x45,0x3E,0x00,0x00}, {0x00,0x42,0x7F,0x40,0x00,0x00,0x00},
    {0x42,0x61,0x51,0x49,0x46,0x00,0x00}, {0x21,0x41,0x45,0x4B,0x31,0x00,0x00},
    {0x18,0x14,0x12,0x7F,0x10,0x00,0x00}, {0x27,0x45,0x45,0x45,0x39,0x00,0x00},
    {0x3C,0x4A,0x49,0x49,0x30,0x00,0x00}, {0x01,0x71,0x09,0x05,0x03,0x00,0x00},
    {0x36,0x49,0x49,0x49,0x36,0x00,0x00}, {0x06,0x49,0x49,0x29,0x1E,0x00,0x00},
    {0x00,0x36,0x36,0x00,0x00,0x00,0x00}, {0x00,0x56,0x36,0x00,0x00,0x00,0x00},
    {0x00,0x08,0x14,0x22,0x41,0x00,0x00}, {0x14,0x14,0x14,0x14,0x14,0x00,0x00},
    {0x41,0x22,0x14,0x08,0x00,0x00,0x00}, {0x02,0x01,0x51,0x09,0x06,0x00,0x00},
    {0x32,0x49,0x79,0x41,0x3E,0x00,0x00}, {0x7E,0x11,0x11,0x11,0x7E,0x00,0x00},
    {0x7F,0x49,0x49,0x49,0x36,0x00,0x00}, {0x3E,0x41,0x41,0x41,0x22,0x00,0x00},
    {0x7F,0x41,0x41,0x22,0x1C,0x00,0x00}, {0x7F,0x49,0x49,0x49,0x41,0x00,0x00},
    {0x7F,0x09,0x09,0x01,0x01,0x00,0x00}, {0x3E,0x41,0x41,0x51,0x32,0x00,0x00},
    {0x7F,0x08,0x08,0x08,0x7F,0x00,0x00}, {0x00,0x41,0x7F,0x41,0x00,0x00,0x00},
    {0x20,0x40,0x41,0x3F,0x01,0x00,0x00}, {0x7F,0x08,0x14,0x22,0x41,0x00,0x00},
    {0x7F,0x40,0x40,0x40,0x40,0x00,0x00}, {0x7F,0x02,0x04,0x02,0x7F,0x00,0x00},
    {0x7F,0x04,0x08,0x10,0x7F,0x00,0x00}, {0x3E,0x41,0x41,0x41,0x3E,0x00,0x00},
    {0x7F,0x09,0x09,0x09,0x06,0x00,0x00}, {0x3E,0x41,0x51,0x21,0x5E,0x00,0x00},
    {0x7F,0x09,0x19,0x29,0x46,0x00,0x00}, {0x46,0x49,0x49,0x49,0x31,0x00,0x00},
    {0x01,0x01,0x7F,0x01,0x01,0x00,0x00}, {0x3F,0x40,0x40,0x40,0x3F,0x00,0x00},
    {0x1F,0x20,0x40,0x20,0x1F,0x00,0x00}, {0x7F,0x20,0x18,0x20,0x7F,0x00,0x00},
    {0x63,0x14,0x08,0x14,0x63,0x00,0x00}, {0x03,0x04,0x78,0x04,0x03,0x00,0x00},
    {0x61,0x51,0x49,0x45,0x43,0x00,0x00}, {0x00,0x00,0x7F,0x41,0x41,0x00,0x00},
    {0x02,0x04,0x08,0x10,0x20,0x00,0x00}, {0x41,0x41,0x7F,0x00,0x00,0x00,0x00},
    {0x04,0x02,0x01,0x02,0x04,0x00,0x00}, {0x40,0x40,0x40,0x40,0x40,0x00,0x00},
    {0x00,0x01,0x02,0x04,0x00,0x00,0x00}, {0x20,0x54,0x54,0x54,0x78,0x00,0x00},
    {0x7F,0x48,0x44,0x44,0x38,0x00,0x00}, {0x38,0x44,0x44,0x44,0x20,0x00,0x00},
    {0x38,0x44,0x44,0x48,0x7F,0x00,0x00}, {0x38,0x54,0x54,0x54,0x18,0x00,0x00},
    {0x08,0x7E,0x09,0x01,0x02,0x00,0x00}, {0x08,0x14,0x54,0x54,0x3C,0x00,0x00},
    {0x7F,0x08,0x04,0x04,0x78,0x00,0x00}, {0x00,0x44,0x7D,0x40,0x00,0x00,0x00},
    {0x20,0x40,0x44,0x3D,0x00,0x00,0x00}, {0x00,0x7F,0x10,0x28,0x44,0x00,0x00},
    {0x00,0x41,0x7F,0x40,0x00,0x00,0x00}, {0x7C,0x04,0x18,0x04,0x78,0x00,0x00},
    {0x7C,0x08,0x04,0x04,0x78,0x00,0x00}, {0x38,0x44,0x44,0x44,0x38,0x00,0x00},
    {0x7C,0x14,0x14,0x14,0x08,0x00,0x00}, {0x08,0x14,0x14,0x18,0x7C,0x00,0x00},
    {0x7C,0x08,0x04,0x04,0x08,0x00,0x00}, {0x48,0x54,0x54,0x54,0x20,0x00,0x00},
    {0x04,0x3F,0x44,0x40,0x20,0x00,0x00}, {0x3C,0x40,0x40,0x20,0x7C,0x00,0x00},
    {0x1C,0x20,0x40,0x20,0x1C,0x00,0x00}, {0x3C,0x40,0x30,0x40,0x3C,0x00,0x00},
    {0x44,0x28,0x10,0x28,0x44,0x00,0x00}, {0x0C,0x50,0x50,0x50,0x3C,0x00,0x00},
    {0x44,0x64,0x54,0x4C,0x44,0x00,0x00}, {0x00,0x08,0x36,0x41,0x00,0x00,0x00},
    {0x00,0x00,0x7F,0x00,0x00,0x00,0x00}, {0x00,0x41,0x36,0x08,0x00,0x00,0x00},
    {0x02,0x01,0x02,0x04,0x02,0x00,0x00},
};

#define ASCII_W 5
#define ASCII_H 7
#define ASCII_ADV (ASCII_W + 1)   // 6

#define CJK_PX 12
#define CJK_ADV (CJK_PX + 1)      // 13

// Decode one UTF-8 codepoint starting at *i; advance *i. Returns codepoint.
static uint32_t utf8_next(const char* s, size_t len, size_t* i) {
    if (*i >= len) return 0;
    unsigned char c = (unsigned char)s[*i];
    if (c < 0x80) { (*i)++; return c; }
    if ((c & 0xE0) == 0xC0 && *i + 1 < len) {
        uint32_t cp = ((c & 0x1F) << 6) | ((unsigned char)s[*i+1] & 0x3F);
        *i += 2; return cp;
    }
    if ((c & 0xF0) == 0xE0 && *i + 2 < len) {
        uint32_t cp = ((c & 0x0F) << 12) | (((unsigned char)s[*i+1] & 0x3F) << 6)
                    | ((unsigned char)s[*i+2] & 0x3F);
        *i += 3; return cp;
    }
    if ((c & 0xF8) == 0xF0 && *i + 3 < len) {
        uint32_t cp = ((c & 0x07) << 18) | (((unsigned char)s[*i+1] & 0x3F) << 12)
                    | (((unsigned char)s[*i+2] & 0x3F) << 6) | ((unsigned char)s[*i+3] & 0x3F);
        *i += 4; return cp;
    }
    (*i)++;
    return '?';
}

static int char_advance(uint32_t cp) {
    if (cp < 0x80) return ASCII_ADV;
    return CJK_ADV;
}

static void put_pixel(render_ctx* r, int x, int y, uint32_t c) {
    if (x < 0 || y < 0 || x >= r->w || y >= r->h) return;
    r->buf[y * r->w + x] = c;
}

static void draw_ascii(render_ctx* r, char ch, int dx, int dy, uint32_t fg) {
    if (ch < 32 || ch > 126) ch = '?';
    const uint8_t* g = FONT[ch - 32];
    for (int row = 0; row < ASCII_H; row++) {
        uint8_t bits = g[row];
        for (int col = 0; col < ASCII_W; col++) {
            if (bits & (0x10 >> col)) put_pixel(r, dx + col, dy + row, fg);
        }
    }
}

static void draw_cjk(render_ctx* r, uint32_t cp, int dx, int dy, uint32_t fg) {
    int gw = 0, gh = 0;
    uint8_t* bits = platform_get_glyph(cp, CJK_PX, &gw, &gh);
    if (!bits || gw <= 0 || gh <= 0) {
        // fallback: draw a box
        for (int y = 0; y < CJK_PX; y++)
            for (int x = 0; x < CJK_PX; x++)
                if (x == 0 || y == 0 || x == CJK_PX-1 || y == CJK_PX-1)
                    put_pixel(r, dx + x, dy + y, fg);
        if (bits) moyu_free(bits);
        return;
    }
    int row_bytes = ((gw + 31) / 32) * 4;
    for (int y = 0; y < gh; y++) {
        for (int x = 0; x < gw; x++) {
            uint8_t byte = bits[y * row_bytes + (x / 8)];
            if (byte & (0x80 >> (x % 8))) {
                put_pixel(r, dx + x, dy + y, fg);
            }
        }
    }
    moyu_free(bits);
}

static void draw_codepoint(render_ctx* r, uint32_t cp, int dx, int dy, uint32_t fg) {
    if (cp < 0x80) draw_ascii(r, (char)cp, dx, dy, fg);
    else draw_cjk(r, cp, dx, dy, fg);
}

bubble_rect render_bubble(render_ctx* r, const char* text, int dx, int dy_top) {
    bubble_rect out = {0,0,0,0};
    if (!text) return out;
    size_t len = strlen(text);
    if (len == 0) return out;

    // Compute text width & wrap into lines.
    // Max bubble width = min(r->w - 8, 200).
    int max_w = r->w - 8;
    if (max_w > 200) max_w = 200;
    if (max_w < 40) max_w = 40;

    // Tokenize into codepoints, then greedily wrap.
    typedef struct { int x; uint32_t cp; } tok;
    tok* toks = (tok*)moyu_alloc(len * sizeof(tok));
    int ntok = 0;
    {
        size_t i = 0;
        int x = 0;
        while (i < len) {
            uint32_t cp = utf8_next(text, len, &i);
            toks[ntok].cp = cp;
            toks[ntok].x = x;
            x += char_advance(cp);
            ntok++;
        }
    }
    int total_w = ntok > 0 ? (toks[ntok-1].x + char_advance(toks[ntok-1].cp)) : 0;

    // Greedy wrap: break when next token would exceed max_w; also break on '\n'.
    int* line_start = (int*)moyu_alloc((ntok + 1) * sizeof(int));
    int* line_end = (int*)moyu_alloc((ntok + 1) * sizeof(int));
    int nlines = 0;
    int i = 0;
    while (i < ntok) {
        int start = i;
        int x = 0;
        while (i < ntok) {
            if (toks[i].cp == '\n') { i++; break; }
            int adv = char_advance(toks[i].cp);
            if (x + adv > max_w && i > start) break;
            x += adv;
            i++;
        }
        line_start[nlines] = start;
        line_end[nlines] = i;
        nlines++;
    }

    int pad = 4;
    int line_h = CJK_PX + 2;   // CJK is the taller of the two
    int text_h = nlines * line_h;
    int bw = (total_w < max_w ? total_w : max_w) + pad * 2;
    int bh = text_h + pad * 2;

    int bx = dx - bw / 2;
    int by = dy_top - bh;       // bubble above dy_top
    if (by < 0) by = dy_top + 4;  // flip below

    out.x = bx; out.y = by; out.w = bw; out.h = bh;

    // Bubble background (opaque white) + dark border.
    uint32_t bg     = 0xFFFFFFFFu;
    uint32_t border = 0x303038FFu;
    for (int y = 0; y < bh; y++) {
        for (int x = 0; x < bw; x++) {
            int tx = bx + x, ty = by + y;
            if (tx < 0 || ty < 0 || tx >= r->w || ty >= r->h) continue;
            uint32_t c = bg;
            if (x == 0 || y == 0 || x == bw - 1 || y == bh - 1) c = border;
            r->buf[ty * r->w + tx] = c;
        }
    }

    uint32_t fg = 0x202028FFu;
    for (int li = 0; li < nlines; li++) {
        int tx = bx + pad;
        int ty = by + pad + 2;  // small top inset
        // Center line horizontally if narrower than bubble
        int line_w = 0;
        for (int k = line_start[li]; k < line_end[li]; k++)
            line_w += char_advance(toks[k].cp);
        tx += (bw - pad * 2 - line_w) / 2;
        int base_y = ty + li * line_h;
        for (int k = line_start[li]; k < line_end[li]; k++) {
            uint32_t cp = toks[k].cp;
            // Vertical: ASCII glyphs are 7px tall, CJK 12px. Align to common top+2.
            int yoff = (cp < 0x80) ? 2 : 0;
            draw_codepoint(r, cp, tx, base_y + yoff, fg);
            tx += char_advance(cp);
        }
    }

    moyu_free(toks);
    moyu_free(line_start);
    moyu_free(line_end);
    return out;
}
